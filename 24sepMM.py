# terminary if else 
# a=5
# 'even' if a%2==0 else 'odd' ---> this only returns the value
# age = int(input("enter age"))
# b = 'eligible' if age>=18 else 'not eligible'
# print(b)

# global scope ----> use global keyword in func to make the var global
# local scope  ---> defined in a function
# enclosing scope  ---> function in a function var